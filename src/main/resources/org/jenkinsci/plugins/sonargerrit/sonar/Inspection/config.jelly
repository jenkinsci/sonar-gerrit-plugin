<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

  <f:entry title="${%jenkins.plugin.settings.inspection.sonar-qube.installation}" field="sonarQubeInstallationName">
    <f:select/>
  </f:entry>

  <f:entry title="${%jenkins.plugin.settings.inspection.analysis-strategy}">
    <f:hetero-radio field="analysisStrategy" descriptors="${descriptor.analysisStrategyDescriptors}"/>
  </f:entry>

</j:jelly>
